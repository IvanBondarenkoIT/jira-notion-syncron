# Как обновить задачи в Jira полным содержимым из Notion

## Скрипт готов! ✅

Создан скрипт `scripts/update_all_jira_tasks.py`, который:

1. Загружает все данные из локального хранилища Notion (`data/notion_export/`)
2. Проходит по всем задачам в Jira (DG-1 до DG-200)
3. Находит соответствующую страницу в Notion по названию
4. Обновляет описание задачи полным содержимым:
   - ✅ Полный текст задачи
   - ✅ Чеклисты (todo-items) с состоянием
   - ✅ Изображения (ссылки)
   - ✅ Файлы (ссылки)
   - ✅ Ссылки из текста
   - ✅ Метаданные и ссылка на Notion

## Что было проверено (DRY RUN):

```
✅ 156 задач - готовы к обновлению
⚠️  4 задачи - пропущены (нет соответствия в Notion)
📝 40 задач - не найдены (номера не существуют в Jira)
❌ 0 ошибок
```

## Как запустить обновление:

### Шаг 1: Откройте скрипт

```powershell
notepad scripts\update_all_jira_tasks.py
```

### Шаг 2: Измените настройку

Найдите строку (около строки 183):
```python
dry_run = True  # Set to False to actually update
```

Измените на:
```python
dry_run = False  # REAL UPDATE MODE
```

### Шаг 3: Запустите скрипт

```powershell
venv\Scripts\python.exe scripts\update_all_jira_tasks.py
```

### Шаг 4: Дождитесь завершения

Скрипт обновит все 156 задач (займет 2-3 минуты).

Вы увидите:
```
✓ Updated DG-94
✓ Updated DG-95
✓ Updated DG-96
...
```

## Что будет обновлено:

### До обновления:
```
Задача DG-150: Кофейные карты
Описание: (пусто или минимальное)
Labels: notion-import
```

### После обновления:
```
Задача DG-150: Кофейные карты

## Описание из Notion
## Меняем летние на зимние

## Sub-tasks
- НАЧАЛА ДЕЛАТЬ
- ВОПРОС ПО РАФУ И ТЫКВЕННЫМ НАПИТКАМ
...

## Чеклист
☑ НАЧАЛА ДЕЛАТЬ
☐ ВОПРОС ПО РАФУ
☐ Ждем от Ирины окончательные цены
...

---
Импортировано из Notion • Оригинал

Labels: notion-import, full-content-updated, has-checklist
```

## Безопасность:

✅ **Скрипт безопасен:**
- Только обновляет существующие задачи
- Не удаляет и не создает новые задачи
- Сохраняет все существующие labels
- Добавляет новые labels (`full-content-updated`, `has-checklist`, `has-images`)

✅ **Можно откатить:**
- В Jira есть история изменений
- Можно посмотреть предыдущие версии описаний

## Настройки скрипта:

В файле `scripts/update_all_jira_tasks.py` можно изменить:

```python
# Настройки (строки 176-180)
dry_run = True          # False для реального обновления
start_num = 1          # Начальный номер задачи
end_num = 200          # Конечный номер задачи
```

### Примеры:

**Обновить только одну задачу:**
```python
start_num = 150
end_num = 150
dry_run = False
```

**Обновить диапазон:**
```python
start_num = 94
end_num = 160
dry_run = False
```

**Обновить все:**
```python
start_num = 1
end_num = 200
dry_run = False
```

## Проверка результата:

После обновления откройте любую задачу в Jira:
```
https://dimkavageorgia.atlassian.net/browse/DG-150
```

Вы должны увидеть:
- ✅ Полное описание задачи
- ✅ Чеклисты с галочками
- ✅ Ссылки на изображения
- ✅ Ссылку на оригинал в Notion
- ✅ Новые labels

## Часто задаваемые вопросы:

**Q: Что если задача уже была обновлена?**
A: Скрипт перезапишет описание новой версией.

**Q: Потеряются ли комментарии?**
A: Нет, комментарии не затрагиваются.

**Q: Что если в Notion изменилось название?**
A: Задача не будет обновлена (по названию не найдется соответствие).

**Q: Можно ли обновить только новые задачи?**
A: Да, измените `start_num` на номер первой новой задачи.

**Q: Сколько времени это займет?**
A: ~1-2 секунды на задачу, итого ~3-5 минут для 156 задач.

## После обновления:

1. Проверьте несколько задач в Jira
2. Убедитесь что содержимое корректно
3. Можете добавить label filter в Jira:
   - `full-content-updated` - обновленные задачи
   - `has-checklist` - задачи с чеклистами
   - `has-images` - задачи с изображениями

## Полезные команды:

**Посмотреть dry-run снова:**
```powershell
venv\Scripts\python.exe scripts\update_all_jira_tasks.py
```

**Обновить конкретную задачу (после изменения скрипта):**
```powershell
venv\Scripts\python.exe scripts\update_all_jira_tasks.py
```

**Проверить одну задачу:**
```powershell
venv\Scripts\python.exe scripts\update_jira_simple.py
```

---

**Готово! Можете запускать обновление когда будете готовы! 🚀**







